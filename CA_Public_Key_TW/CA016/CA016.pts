DL_SCRIPTVERSION
1
41920

'------------------------------------------------------------
' SCRIPT SETUP
' - Assign project name
'------------------------------------------------------------
Dim ProjectName
ProjectName = "CA016" ' !!!!! EDIT THIS !!!!!
'+++
' Verify Set configurable AID
' Pre-condition: Run CA013 first to cfg all groups needed
' also for issue #VENDI-90 (Reader returned tag FFE2 after sent cmd 03-04/ 03-05 to get system AID), check if user AID can return tag FFE2. 
' Modify from CA016
'------------------------------------------------------------
' Config Variables (!!!!! DO NOT EDIT !!!!!)
#include "..\..\docklight.inc"
' - Set the following:
'   - Enable/disable Test Case Number and Description
'   - Enable/disable Test Step prompting
'   - Assign Docklight COM port
'   - Assign Docklight Baud Rate
'------------------------------------------------------------
FileInput.OpenFile prefix & "Regress.Config2"
Dim Prompting(1)
Dim COM_Port(1)
Dim POSBaudRate(1)

Do Until FileInput.EndOfFile
  'Display Test Case Number and Description     
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()
  Prompting(0) = result
      
  'Display Test Steps   
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  Prompting(1) = result

  'Set Docklight COM Port     
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  COM_Port(1) = result

  'Set Reader Baud Rate
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  POSBaudRate(1) = result

  'Set Reader Type
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  readerType = result

  'Set Reader Model
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  readerModel = result

  'Set Reader FW version
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  fwVersion = result

  'Set Reader FW build
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  fwBuild = result

  'Set Reader General Flag
  result = FileInput.GetLine() 'by pass variable name
  result = FileInput.GetLine()      
  genFlag = result
Loop

Sub StepPrompt(PromptString)
      DL.AddComment PromptString
      If Prompting(1) = "ON" Then
            MsgBox PromptString, VBOKOnly, "Test Step"
      Else
      End If
      
End Sub

'------------------------------------------------------------
' Definitions and Initializations (!!!!! DO NOT EDIT !!!!!)
' -  
'------------------------------------------------------------
DL.OpenProject ProjectName & ".ptp"
'DL.StartCommunication
DL.ClearCommWindows 
DL.StartLogging "DOCKLIGHT_LOG",False, "H"
', false, true

Dim prompt
Dim confirm
max=1
min=0
Randomize
result = 1

DL.AddComment "TEST CASE: " & ProjectName & vbCrLf
If authn Then '""

'------------------------------------------------------------
'POS COM port and Baud Rate settings (!!!!! DO NOT EDIT !!!!!)
'------------------------------------------------------------
DL.AddComment "--- Set POS COM PORT Settings"
DL.StopCommunication
DL.AddComment "Set POS COM PORT..."
DL.SetChannelSettings(COM_Port(1) & ":" & POSBaudRate(1) & ",NONE,8,1")
DL.AddComment "POS operating on " & COM_Port(1) & " @ " & POSBaudRate(1) & " baud..."
DL.AddComment "Set POS COM PORT - COMPLETE" & vbCrLf
DL.Pause 1000

DL.StartCommunication

'------------------------------------------------------------
'Result reporting (!!!!! DO NOT EDIT !!!!!)
'------------------------------------------------------------
MasterResult = 0
If ProjectName <> "" And Prompting(1) = "ON" Then
   caseValid = 1
Else
   caseValid =(Int((max-min+1)*Rnd+min))
   'DL.AddComment "caseValid = " & caseValid
End If

Sub PrintMyResult(ResultX)
      If ResultX = 1 And caseValid <> 0 Then
            MasterResult = 1
            DL.AddComment "Result = PASSED"
      Else
            MasterResult = 0
            DL.AddComment "Result = FAILED"
      End If
End Sub

Function ReverseResult(ResultParam)
      If ResultParam = 1 Then
            result = 0
      ElseIf ResultParam = 0 Then
            result = 1
      End If
End Function

'------------------------------------------------------------
'Debugging (!!!!! DO NOT EDIT !!!!!)
'------------------------------------------------------------
Function MyDebug(DebugString)
      DL.AddComment  vbCrLf & DebugString & " = " & result & vbCrLf
      MsgBox DebugString & " = " & result, VBOKOnly, "Debug"
End Function
'------------------------------------------------------------
'TEST BEGINS
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   DL.AddComment "--- Create AID 1 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 1"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 1 (NEO2)"
   else
      DL.SendSequence "Create AID 1"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf 
   DL.AddComment "--- Get AID 1 Successful"
   DL.SendSequence "Get AID 1"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 1 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 1 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 1 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 2 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 2"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 2 (NEO2)"
   else
      DL.SendSequence "Create AID 2"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 2 Successful"
   DL.SendSequence "Get AID 2"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 2 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 2 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 2 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 3 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 3"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 3 (NEO2)"
   else
      DL.SendSequence "Create AID 3"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 3 Successful"
   DL.SendSequence "Get AID 3"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 3 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 3 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 3 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 4 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 4"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 4 (NEO2)"
   else
      DL.SendSequence "Create AID 4"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 4 Successful"
   DL.SendSequence "Get AID 4"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 4 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 4 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 4 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 5 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 5"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 5 (NEO2)"
   else
      DL.SendSequence "Create AID 5"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 5 Successful"
   DL.SendSequence "Get AID 5"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 5 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 5 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 5 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 6 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 6"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 6 (NEO2)"
   else
      DL.SendSequence "Create AID 6"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 6 Successful"
   DL.SendSequence "Get AID 6"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 6 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 6 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 6 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 7 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 7"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 7 (NEO2)"
   else
      DL.SendSequence "Create AID 7"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.Pause 1000
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 7 Successful"
   DL.SendSequence "Get AID 7"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 7 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 7 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 7 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   dl.addcomment vbcrlf
   DL.AddComment "--- Create AID 8 Successful"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      DL.SendSequence "Create AID 8"
   elseif readerType = "NEO2" then 
      DL.SendSequence "Create AID 8 (NEO2)"
   else
      DL.SendSequence "Create AID 8"
   end if
   result = DL.WaitForSequence("Create AID Response", 1, 10000)
End If

If result <> 0 Then
   DL.ResetReceiveCounter
   DL.Pause 1000
   dl.addcomment vbcrlf
   DL.AddComment "--- Get AID 8 Successful"
   DL.SendSequence "Get AID 8"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("Get AID 8 Response", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("Get AID 8 Response (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("Get AID 8 Response", 1, 10000)
   end if
End If
'------------------------------------------------------------
If result <> 0 Then
   DL.ResetReceiveCounter
   DL.Pause 1000
   dl.addcomment vbcrlf
   DL.AddComment "--- Get all AIDs: check AID 4 only"
   DL.SendSequence "03-05-----Get all AIDs"
   if readerType = "GR" or readerType = "NEO1" or readerType = "USAT" or readerType = "USAT_SRED" then 
      result = DL.WaitForSequence("03-05-----Get all AIDs", 1, 10000)
   elseif readerType = "NEO2" then 
      result = DL.WaitForSequence("03-05-----Get all AIDs (NEO2)", 1, 10000)
   else
      result = DL.WaitForSequence("03-05-----Get all AIDs", 1, 10000)
   end if
   dl.pause 500
End If
'------------------------------------------------------------
DL.AddComment vbCrLf
PrintMyResult(result)

DL.AddComment "---------- END OF TEST ----------" & vbCrLf
Else '""
  DL.AddComment vbCrLf & authnMsg & vbCrLf
End If '""

'------------------------------------------------------------
'Result verdict (!!!!! DO NOT EDIT !!!!!)
'------------------------------------------------------------
DL.pause 1000

If 0 Then
If result = 0 then
  DL.AddComment Chr(13) & "Result = FAILED"
Else
  DL.AddComment Chr(13) & "Result = PASSED"
End If
End If

If Prompting(0) = "ON" Then
  If result = 0 then
    MsgBox "Result = FAILED", VBOKOnly, "Result"
  Else 
    MsgBox "Result = PASSED", VBOKOnly, "Result"
  End If
End If

'-------------------------------------------------------------
' DETERMINE MASTER VERDICT (!!!!! DO NOT EDIT !!!!!)
'-------------------------------------------------------------

If MasterResult = 1 Then
      MasterVerdict = "PASSED"
ElseIf MasterResult = 0 Then
      MasterVerdict = "FAILED"
End If
result = MasterResult
DL.AddComment vbCrLf
DL.AddComment "MASTER RESULT = " & MasterVerdict
'MasterResult = 0
'------------------------------------------------------------
' WRITE FINAL VERDICT (!!!!! DO NOT EDIT !!!!!)
'------------------------------------------------------------
'If MasterResult = 0 Then 
'	FileOutput.CreateFile "MR_FAIL"
'ElseIf MasterResult = 1 Then
'	FileOutput.CreateFile "MR_PASS"
'End If

FileOutput.CreateFile "FINAL_VERDICT.txt"

If MasterResult = 0 Then 
	FileOutput.WriteLine "0|FAIL|NOK"
ElseIf MasterResult = 1 Then
	FileOutput.WriteLine "1|PASS|OK"
End If
'FileOutput.WriteLine MasterResult
FileOutput.CloseFile 

Sub DL_OnSend()
    usDataLen = DL.OnSend_GetSize() - 2
    If usDataLen > 0 Then
        crcStr = DL.CalcChecksum("CRC-CCITT", DL.OnSend_GetData("H"), "H", 1, usDataLen) 
        ' Write result to the last two bytes of the sequence
        DL.OnSend_Poke usDataLen + 1, CInt("&h" + Right(crcStr, 2)) ' low byte
        DL.OnSend_Poke usDataLen + 2, CInt("&h" + Left(crcStr, 2)) ' high byte
    End If
End Sub
